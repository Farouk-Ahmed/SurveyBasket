# SurveyBasket.API

Run with the **https** launch profile so the API is available at:

- **https://localhost:7270**

The frontend (SurveyBasket-UI) uses this URL in `src/app/core/config/api.config.ts` (`API_BASE_URL`). Keep both in sync when changing the port.
